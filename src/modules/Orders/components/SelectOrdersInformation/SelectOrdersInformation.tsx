import styles from './SelectOrdersInformation.module.scss';

export const SelectOrdersInformation = () => {
  return (
    <div className={styles['orders-information']}>
      <p>Please select the type of orders</p>
    </div>
  );
};
